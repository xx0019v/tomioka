# 06 動作確認結果

検証日時: 2026-08-04（JST）  
基準commit: `24ded911bce6f6d14471cadb97cae9126f8ea754`  
検証環境: macOS、Node.js 26.4.0、npm 11.17.0

## クリーン環境

指定commitを`git archive`で`/tmp`の新規ディレクトリへ展開し、提出ブランチで補正した依存定義だけを適用して検証しました。既存`node_modules`、`.next`、`out`は使用していません。

| 項目 | 結果 |
| --- | --- |
| `npm ci` | 成功、355 packages |
| `npm run lint` | 成功、エラー0 |
| `npm run typecheck` | 成功、エラー0 |
| GitHub Pages用`npm run build` | 成功、静的9ページ生成 |
| 出力先 | `out/` |

採用commitの元lockfileは`gsap`、`three`、`@types/three`が不足していたため、提出ブランチで宣言とlockを同期しました。製品コード、表示、座標は変更していません。

## 教員フィードバックE2E

`e2e/teacher-feedback.spec.mjs`をChromiumとWebKitで実行し、8/8成功しました。

- 320、375、390、430、768、1024、1280、1440、1920px
- Section 3の番号と縦線
- 施設名の改行
- 駅から入口までの線
- 正式URL、canonical、OGP
- 3施設の座標、ピン先端のアニメーション中固定
- モバイル地図下端
- 横オーバーフロー
- 地図一覧・詳細操作

## 公開ビルドHTTPスモーク

`out/`を`/tomioka`配下へHTTP配信し、Chromium / WebKitで確認しました。

- `/tomioka/`、`/information/`、`/map/`: HTTP 200
- 3ページとも横オーバーフロー0px、壊れた画像0件
- アプリ由来のconsole error 0件、同一オリジンHTTP 4xx/5xx 0件
- Three.js canvas: `#route`内に1枚生成
- CTA: `/tomioka/map/`へ設定
- Leaflet: 表示、マーカー6件
- カフェドローム詳細: 操作後に表示
- 存在しないURL: HTTP 404

ChromiumのNext.js router prefetchには画面遷移時の`ERR_ABORTED`がありましたが、意図された先読み取消で、ページ・資産の失敗ではありません。

## 旧v1.1 QAスイート

`e2e/v11-release-qa.spec.mjs`は現在のDOMと異なる内部実装を前提にした判定が残っており、非ゲート扱いです。全9幅の基本表示ゲートはChromeで成功しましたが、絹糸の内部request回数を厳密に`+1`とする判定、およびWebKitで外部リンク文字列を1行固定する判定が失敗しました。現行の先生フィードバックE2Eと公開ビルドスモークでは該当機能が成功しています。

## 未検証

- 物理iPhone / 物理Android端末
- iOS Safariの実アドレスバー伸縮、低電力モード、実機発熱
- 位置情報を屋外の実座標で許可した状態
- 正式案内URL側サーバーへの最新版反映
